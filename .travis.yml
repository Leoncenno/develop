language: python

python:
  - "3.8"

services:
  - postgresql

env:
  - APP_MODE=TEST

before_script:
  - psql -c 'create database StackOverflow;' -U postgres
  - psql -c 'INSERT INTO user_details (Ronald, Okello, ronald@gmail.com, ron, 1994-02-04, ronaldo);'


install:
  - pip3 install -r requirements.txt

script:
  - pytest test.py
  - pytest --cov=app tests/
  - coverage run -m pytest
 
after_success:
  - coveralls
  - coverage report -m